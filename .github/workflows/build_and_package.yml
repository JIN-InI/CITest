name: build-and-package
on: [push]
jobs:
  build_and_upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          submodules: 'recursive'
      
      # setup-javaは依存関係のcasheもしてくれる
      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: ${{ secrets.JAVA_VERSION }}
          cache: 'maven'
          distribution: 'adopt'
      
      - name: Build with Maven and copy artifact
        run: mvn -B package -f pom.xml && mkdir ~/target && cp target/*.jar target

      - name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: Package
          path: target
          
